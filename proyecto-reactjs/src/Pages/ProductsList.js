import ItemListContainer from "../Components/ItemContainer/ItemListContainer";

const ProductsList = () => {
    
    return(
        <ItemListContainer />
    )
};

export default ProductsList;

//probar si agregando como prop la coleccion que quiero traer se puede. y no tener que hacer 2 veces lo mismo con diferentes colecciones.
//crear la page para joyas y otra para accesorios.
//cargar todos los acc a firebase